general {
	lock_cmd = pidof hyprlock || uwsm-app -- hyprlock
	before_sleep_cmd = pidof hyprlock || uwsm-app -- hyprlock
	after_sleep_cmd = hyprctl dispatch dpms on
	inhibit_sleep = 3
}

listener {
	timeout = 300 # 5 mins
	on-timeout = loginctl lock-session
}

listener {
	timeout = 600 # 10 mins
	on-timeout = systemctl suspend
}
